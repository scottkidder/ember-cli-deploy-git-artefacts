{
  "name": "ember-cli-deploy-git-artefacts",
  "version": "0.1.0",
  "description": "This plugin generates git artefacts into files",
  "keywords": [
    "ember-addon",
    "ember-cli-deploy-plugin"
  ],
  "license": "MIT",
  "author": "",
  "repository": "https://github.com/scottkidder/ember-cli-deploy-git-artefacts",
  "scripts": {
    "lint:js": "./node_modules/.bin/eslint ./*.js config"
  },
  "dependencies": {
    "ember-cli-deploy-plugin": "~0.2.9",
    "git-rev-sync": "^1.12.0"
  },
  "devDependencies": {
    "ember-cli": "~3.1.4",
    "eslint-plugin-node": "^6.0.1"
  },
  "engines": {
    "node": "^4.5 || 6.* || >= 7.*"
  }
}
